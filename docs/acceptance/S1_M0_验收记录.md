# S1-M0 验收记录

- 验收日期：2026-08-11
- 工作区：`ai_robot_ros2`
- 范围：工程与管理基线
- 已验收代码基线：`67fd9be`
- 结论：通过；以包含本记录及配套脚本、资源卡的 Git 提交作为 S1-M0 最终基线

## 环境核验

| 检查项 | 实测结果 | 状态 |
|---|---|---|
| 操作系统 | Ubuntu 22.04.5 LTS，内核 `6.8.0-136-generic` | 通过 |
| ROS 2 | Humble，`/opt/ros/humble` | 通过 |
| 构建与依赖工具 | `ros2`、`colcon`、`rosdep 0.26.0`、Git 可用 | 通过 |
| 磁盘 | 工作区所在文件系统约 208 GiB 可用 | 通过 |
| 图形设备 | NVIDIA VGA 设备已检测 | 通过 |
| RViz2 | GUI 由操作者人工启动并正常关闭 | 通过 |
| Gazebo | Fortress 6.18.0，GUI 显示 `shapes.sdf` 并正常关闭 | 通过 |
| ROS/Gazebo 集成 | `ros_gz` 0.244.25，`ros_gz_sim`、`ros_gz_bridge` 可用 | 通过 |
| 网络与代理 | apt/rosdep 安装与依赖解析已完成；当前 shell 未设置 HTTP/HTTPS 代理 | 通过 |

环境自检命令：

```bash
bash scripts/environment_check.sh
```

## 工程验证

| 验收项 | 命令或证据 | 结果 |
|---|---|---|
| ROS 依赖 | `rosdep check --from-paths src --ignore-src --rosdistro humble` | `All system dependencies have been satisfied` |
| 构建 | `bash scripts/build.sh` | 3 个软件包构建成功 |
| 自动测试 | `bash scripts/test.sh` | C++ 看门狗 1 项、Python 契约 2 项通过，0 失败 |
| `mode:=sim` | `ros2 launch ai_robot_bringup sim_bringup.launch.py mode:=sim` | 两个基础节点启动并输出 `mode=sim`，无电机输出 |
| `mode:=real` | `ros2 launch ai_robot_bringup sim_bringup.launch.py mode:=real` | 两个基础节点启动并输出 `mode=real`，硬件控制禁用 |
| 非法模式 | `mode:=invalid` | launch 返回非零并提示仅允许 `sim`、`real` |
| 节点停止 | 交互式 Ctrl+C | C++ 与 Python 节点均可完成清理；Python 双信号退出问题已修复 |
| Gazebo Transport | `ign topic -l` | 检出 `/clock` 和 `/world/shapes/*` |
| ROS/Gazebo 桥接 | `ros_gz_bridge` + `ros2 topic echo /clock --once` | 收到 `clock: {sec: 4, nanosec: 447000000}` |

## 交付物核验

- ROS 2 工作区和三个基础软件包已建立。
- `scripts/environment_check.sh`、`scripts/build.sh`、`scripts/test.sh` 提供统一入口。
- 最小 C++/Python 节点、命令看门狗逻辑和模式契约测试已建立。
- `mode:=sim|real` 启动参数存在，上层基础节点不执行任何实体控制。
- 架构基线、实体调试安全规则、资源矩阵和资源卡模板已建立。
- Gazebo Fortress / `ros_gz` 资源卡见 `docs/resources/gazebo_fortress.md`。

## 已知问题与阶段边界

- Gazebo GUI 输出 `libEGL warning: egl: failed to create dri2 screen`，本次不影响 GUI 显示或正常关闭。
- 机器同时存在 Gazebo Classic 11；项目基线固定为 Fortress 6.18.0 + `ros_gz`，不得混用两套插件和 launch。
- 受限自动执行环境会阻止 DDS/UDP socket；ROS/Gazebo 桥接已在允许本地通信的执行环境中复测通过。
- 当前只完成 S1-M0。机器人模型、仿真本体、硬件驱动、SLAM、Nav2、感知和 AI Agent 均未实现或验收。
- S1-M1 从包含本记录及配套脚本、资源卡和测试改动的 Git 提交之后开始。

## 回退

- 已验收代码基线：`67fd9be`。
- 代码修复可回退至 `67fd9be`；完整 S1-M0 应回退至包含本记录的最终基线提交。
