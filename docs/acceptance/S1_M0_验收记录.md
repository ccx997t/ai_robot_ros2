# S1-M0 验收记录

- 日期：2026-08-08
- 工作区：`ai_robot_ros2`
- 范围：工程与管理基线

## 已检查环境

| 检查项 | 结果 |
|---|---|
| 系统 | Ubuntu 22.04.5 LTS |
| ROS 2 | Humble，已检测 `/opt/ros/humble/setup.bash` |
| `ros2` / `colcon` / Git | 已检测 |
| 磁盘 | 209 GiB 可用（检查时） |
| RViz2 | 已检测；图形化启动待有桌面会话时人工确认 |
| Gazebo / `gz` | 未检测到；S1-M0 未达到 Gazebo 验收要求 |

## 本次交付

工作区目录、构建测试入口、C++/Python 最小节点、统一模式启动入口、架构基线、资源卡与安全规则。

## 待填写

| 验收项 | 状态 |
|---|---|
| 环境自检脚本 | 通过；ROS Humble、`ros2`、`colcon` 和 RViz2 已检测 |
| `colcon build` | 通过；3 个软件包构建成功 |
| `colcon test` 和单元测试 | 通过；C++ 看门狗测试与 Python 测试均通过 |
| 统一启动入口 | 通过；`sim_bringup.launch.py` 两个节点均输出心跳 |
| RViz2 / Gazebo | RViz2 待图形会话人工确认；Gazebo 缺失，需安装/验证后方可通过 S1-M0 |
| Git 初始基线 | 已创建本地初始提交；工作树干净 |
