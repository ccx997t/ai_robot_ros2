# S1-M2 验收记录

- 验收日期：2026-08-13
- 工作区：`ai_robot_ros2`
- 阶段：仿真本体与底盘单元
- 前置基线：S1-M1提交`4091b0b`
- 结论：未通过；模型、仿真控制和安全链已实施，运动执行仍有阻塞

## 前置条件

| 检查项 | 实测结果 | 状态 |
|---|---|---|
| S1-M0 | 工程与管理基线已验收 | 通过 |
| S1-M1 | 资源、接口与测试基线已验收 | 通过 |
| Ubuntu / ROS 2 | Ubuntu 22.04.5 LTS、ROS 2 Humble | 通过 |
| Gazebo | Fortress 6.18.0 + `ros_gz` | 通过 |
| 描述与TF工具 | Xacro、`robot_state_publisher`、TF2可用 | 通过 |
| 依赖、构建与测试 | `rosdep`、3包构建、9项测试通过 | 通过 |

## M2依赖准入

| 资源 | 当前状态 | M2要求 |
|---|---|---|
| `ros_gz_sim` / `ros_gz_bridge` | 已安装并准入 | 复用 |
| `ros2_control` / `controller_manager` | 2.54.0，已安装并可发现 | 依赖准入通过；控制器加载待模型实现 |
| `diff_drive_controller` | 2.53.3，已安装并可发现 | 运动、限速、超时和里程计待验证 |
| `joint_state_broadcaster` | 2.53.3，已安装并可发现 | 关节状态待验证 |
| `gz_ros2_control` | 0.7.20，已安装并可发现 | Fortress插件准入通过，模型集成待验证 |

已执行的安装命令：

```bash
sudo apt update
sudo apt install \
  ros-humble-ros2-control \
  ros-humble-ros2-controllers \
  ros-humble-gz-ros2-control
```

准确版本、许可证、最小验证和回退信息见`docs/resources/ros2_control.md`。

## 仿真本体

| 验收项 | 当前状态 | 通过标准 |
|---|---|---|
| 机器人描述包 | `ai_robot_description`已建立并安装 | 通过 |
| Xacro模型 | 尺寸、质量、惯量、碰撞体可展开并通过自动检查 | 通过 |
| 差速轮与关节 | 半径0.075 m、轮距0.34 m、左右连续关节 | 通过（运动方向待场景关闭） |
| 传感器安装Frame | 已预留`sensor_link`，未提前实现M3传感器 | 通过 |
| `robot_state_publisher` | 已接入仿真bringup并识别全部模型Frame | 通过 |
| Gazebo world | `m2_test.sdf`可重复无界面启动 | 通过 |
| 模型生成 | `ros_gz_sim create`返回成功 | 通过（运动物理行为待关闭） |

## 差速底盘单元

| 验收项 | 当前状态 | 通过标准 |
|---|---|---|
| `/cmd_vel` | 唯一订阅者为`cmd_vel_safety`，Reliable/Volatile | 通过 |
| `/odom` | `odom_contract_relay`发布，Reliable/Volatile，实测约49.99 Hz | 通过 |
| `odom -> base_link` | 单一控制器TF发布者，Frame可查询 | 通过 |
| 前进/后退 | 前进命令已到控制器入口，但轮速和X位移接近零 | **失败** |
| 原地旋转 | 因同一运动执行阻塞未继续判定 | 待关闭 |
| 速度限制 | 实测`1.0/-2.0`被裁剪为`0.3/-0.8`并诊断WARN | 通过 |
| 命令超时 | 独立单调墙钟0.5秒看门狗，超时持续发布零速度 | 通过 |
| 生命周期/重启 | 两控制器均可加载、激活并在退出时停用 | 通过 |

## 测试与证据

| 测试 | 当前状态 | M2要求 |
|---|---|---|
| Xacro/URDF检查 | 3项描述测试通过 | 通过 |
| TF契约测试 | 规范已建立，尚无运行TF | 检查树连通、单一发布者、频率和时间 |
| 控制器加载测试 | `joint_state_broadcaster`、`base_controller`实测active | 通过 |
| 运动接口测试 | Topic类型、QoS、频率、Frame已实测 | 通过 |
| 安全测试 | 限速、非有限值和看门狗有单元/运行证据 | 通过 |
| 场景测试 | 前进运动失败，后退与旋转不判定 | **失败** |
| rosbag证据 | M1流程已建立 | 记录`/cmd_vel`、`/odom`、`/tf`、`/tf_static`、`/diagnostics` |

## 安全与范围

- 本阶段仅实施仿真底盘，不加载实体驱动，不发送实体电机命令。
- 仿真控制链仍必须验证命令超时、速度限制和唯一底盘出口，为实体迁移建立不可绕过的契约。
- 不提前实现M3传感器、M4融合安全集成、SLAM、Nav2、感知或AI Agent。

## 待关闭项

1. 排查`base_controller`接收到有效命令但车轮速度保持近零的问题。
2. 运动恢复后重新验证前进、后退、原地旋转和停车位姿变化。
3. 将控制器状态、运动、TF和安全场景固化为自动launch测试。
4. 记录M2 rosbag证据。
5. 再次全量执行`rosdep`、构建、测试和仿真场景验收。
6. 提交M2最终Git基线并将结论更新为“通过”。

## 本轮证据摘要

- `bash scripts/build.sh`：5个包构建成功。
- `bash scripts/test.sh`：17项测试通过，0失败。
- 模型创建：`OK creation of entity`。
- 控制器：`joint_state_broadcaster`与`base_controller`均为`active`。
- `/odom`：约49.99 Hz，`header.frame_id=odom`，`child_frame_id=base_link`。
- 失败证据：持续2秒发送`linear.x=0.2`后，X位置约`-4.26e-15`，轮速接近零。

## 回退

- S1-M1最终基线：`4091b0b`。
- M2未通过前，不得将模型占位、控制器安装或单次手动运动视为M2验收完成。
