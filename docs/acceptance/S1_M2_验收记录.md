# S1-M2 验收记录

- 验收日期：2026-08-11
- 工作区：`ai_robot_ros2`
- 阶段：仿真本体与底盘单元
- 前置基线：S1-M1提交`4091b0b`
- 结论：未通过；M2已准入，功能交付物尚未实施

## 前置条件

| 检查项 | 实测结果 | 状态 |
|---|---|---|
| S1-M0 | 工程与管理基线已验收 | 通过 |
| S1-M1 | 资源、接口与测试基线已验收 | 通过 |
| Ubuntu / ROS 2 | Ubuntu 22.04.5 LTS、ROS 2 Humble | 通过 |
| Gazebo | Fortress 6.18.0 + `ros_gz` | 通过 |
| 描述与TF工具 | Xacro、`robot_state_publisher`、TF2可用 | 通过 |
| 依赖、构建与测试 | `rosdep`、3包构建、9项测试通过 | 通过 |

## M2依赖准入

| 资源 | 当前状态 | M2要求 |
|---|---|---|
| `ros_gz_sim` / `ros_gz_bridge` | 已安装并准入 | 复用 |
| `ros2_control` / `controller_manager` | 2.54.0，已安装并可发现 | 依赖准入通过；控制器加载待模型实现 |
| `diff_drive_controller` | 2.53.3，已安装并可发现 | 运动、限速、超时和里程计待验证 |
| `joint_state_broadcaster` | 2.53.3，已安装并可发现 | 关节状态待验证 |
| `gz_ros2_control` | 0.7.20，已安装并可发现 | Fortress插件准入通过，模型集成待验证 |

已执行的安装命令：

```bash
sudo apt update
sudo apt install \
  ros-humble-ros2-control \
  ros-humble-ros2-controllers \
  ros-humble-gz-ros2-control
```

准确版本、许可证、最小验证和回退信息见`docs/resources/ros2_control.md`。

## 仿真本体

| 验收项 | 当前状态 | 通过标准 |
|---|---|---|
| 机器人描述包 | 未建立 | 独立ROS包，安装URDF/Xacro和mesh资源 |
| Xacro模型 | 未建立 | 可展开为有效URDF，尺寸、质量、惯量和碰撞模型明确 |
| 差速轮与关节 | 未建立 | 左右轮轴向、半径、轮距和限制符合配置 |
| 传感器安装Frame | 未建立 | 至少预留规范Frame，不提前实现M3传感器 |
| `robot_state_publisher` | 未接入bringup | 发布`base_link`及静态/关节TF |
| Gazebo world | 未建立 | 最小、可重复启动的测试世界 |
| 模型生成 | 未实现 | Gazebo中成功创建且无明显穿模、漂移或惯量异常 |

## 差速底盘单元

| 验收项 | 当前状态 | 通过标准 |
|---|---|---|
| `/cmd_vel` | 仅冻结契约 | Reliable、KeepLast 1，唯一安全出口接入控制器 |
| `/odom` | 尚无发布者 | Reliable、KeepLast 10，Frame为`odom`/`base_link`，目标≥20 Hz |
| `odom -> base_link` | 尚无发布者 | 单一发布者、时间戳有效、频率达标 |
| 前进/后退 | 未实现 | 方向正确，停止后无持续滑行异常 |
| 原地旋转 | 未实现 | 左右轮方向和角速度符号正确 |
| 速度限制 | 仅有参数基线 | 超限命令被裁剪或拒绝并可诊断 |
| 命令超时 | 仅有普通类测试 | 超过0.5秒无新命令时自动停车 |
| 生命周期/重启 | 未实现 | 控制器可加载、激活、停止和重启 |

## 测试与证据

| 测试 | 当前状态 | M2要求 |
|---|---|---|
| Xacro/URDF检查 | 未建立 | 自动解析、关节和惯量检查 |
| TF契约测试 | 规范已建立，尚无运行TF | 检查树连通、单一发布者、频率和时间 |
| 控制器加载测试 | 未建立 | `controller_manager`状态和控制器激活断言 |
| 运动接口测试 | 未建立 | `/cmd_vel`、`/odom`类型、QoS、频率和Frame |
| 安全测试 | 未建立 | 超时停车、限速、停止和异常命令 |
| 场景测试 | 未建立 | 前进、后退、原地旋转、停车可重复 |
| rosbag证据 | M1流程已建立 | 记录`/cmd_vel`、`/odom`、`/tf`、`/tf_static`、`/diagnostics` |

## 安全与范围

- 本阶段仅实施仿真底盘，不加载实体驱动，不发送实体电机命令。
- 仿真控制链仍必须验证命令超时、速度限制和唯一底盘出口，为实体迁移建立不可绕过的契约。
- 不提前实现M3传感器、M4融合安全集成、SLAM、Nav2、感知或AI Agent。

## 待关闭项

1. 建立机器人描述包和最小Xacro模型。
2. 建立Gazebo测试world和模型生成launch。
3. 配置差速控制器、关节状态和控制参数。
4. 接通受控`/cmd_vel`、`/odom`和`odom -> base_link`。
5. 实现并验证限速、0.5秒命令超时停车和停止行为。
6. 增加URDF、TF、控制器、运动和安全自动测试。
7. 记录M2 rosbag和测试证据。
8. 全量执行`rosdep`、构建、测试和仿真场景验收。
9. 提交M2最终Git基线并将结论更新为“通过”。

## 回退

- S1-M1最终基线：`4091b0b`。
- M2未通过前，不得将模型占位、控制器安装或单次手动运动视为M2验收完成。
