# S1-M3 验收记录

- 验收日期：2026-08-13
- 工作区：`ai_robot_ros2`
- 阶段：仿真传感器单元
- 前置基线：S1-M2关闭提交`2337440`
- 当前已提交基线：`b789710` (`test: validate M3 sensor dynamics`)
- 结论：**通过**；四类对象的桥接、独立入口、诊断、自动接口测试、动态契约和rosbag证据均已关闭

## 范围与通过标准

每类对象必须能独立启动、观察和自动测试，并提供默认参数、诊断配置、rosbag样本及测试记录。M3只实施仿真原始传感器，不提前实施M4的多传感器融合和故障联动。

| 对象 | 公共接口 | 独立验收重点 | 当前状态 |
|---|---|---|---|
| 激光雷达 | `/scan` (`sensor_msgs/msg/LaserScan`) | 角度、距离、频率、噪声、时间戳、BestEffort QoS、`laser_link` | 通过 |
| 相机 | `/camera/image_raw`、`/camera/camera_info` | 分辨率、帧率、编码、参数、时间戳、QoS、`camera_optical_link` | 通过 |
| IMU | `/imu/data` (`sensor_msgs/msg/Imu`) | 角速度、加速度、方向、协方差、频率、BestEffort QoS、`imu_link` | 通过 |
| 仿真编码器 | `/joint_states` (`sensor_msgs/msg/JointState`) | 左右轮方向、累计量、分辨率及与里程计关系 | 独立入口与默认参数通过；动态契约待验证 |

## 传感器Frame与外参

| 验收项 | 实测结果 | 状态 |
|---|---|---|
| 语义Frame | `laser_link`、`camera_link`、`camera_optical_link`、`imu_link`已加入Xacro | 通过 |
| 父子关系 | 全部经`sensor_link`连接到`base_link`，相机光学Frame由`camera_link`派生 | 通过 |
| 坐标约定 | 机身/雷达/IMU遵循REP-103，相机光学Frame为z前、x右、y下 | 通过 |
| 外参冻结 | 数值写入Xacro、TF基线文档并由自动测试精确断言 | 通过 |

## 前置与依赖准入

| 检查项 | 实测结果 | 状态 |
|---|---|---|
| S1-M2 | 提交`2337440`已正式关闭 | 通过 |
| Gazebo | Fortress 6.18.0，Sensors和IMU系统插件可发现 | 通过 |
| ROS桥接 | `ros_gz_bridge`、`ros_gz_image` 0.244.25可发现 | 通过 |
| 图像接口 | `image_transport`、`camera_info_manager` 3.1.13可发现 | 通过 |
| 标准消息 | `sensor_msgs` 4.9.1可发现 | 通过 |
| 依赖检查 | `rosdep check --from-paths src --ignore-src`通过 | 通过 |
| M2回归 | 5包构建成功，19项测试通过、0失败 | 通过 |

版本、许可证、验证方法和回退见`docs/resources/gazebo_sensors.md`。

## 桥接实测

| 接口 | 实测结果 | 状态 |
|---|---|---|
| `/scan` | `LaserScan`，BestEffort/Volatile，`laser_link`，时间戳非零，约9.995 Hz，角度范围±π，最大距离12 m | 通过 |
| `/camera/image_raw` | `Image`，BestEffort/Volatile，`camera_optical_link`，时间戳非零，320×240，`rgb8`，约15.14 Hz | 通过 |
| `/camera/camera_info` | `CameraInfo`，BestEffort/Volatile，`camera_optical_link`，320×240，3×3内参矩阵有效 | 通过 |
| `/imu/data` | `Imu`，BestEffort/Volatile，`imu_link`，时间戳非零，姿态/角速度/线加速度有效，约99.9 Hz | 通过 |
| `/joint_states` | `JointState`，由`joint_state_broadcaster`发布，包含左右轮位置与速度，时间戳非零，约100 Hz | 独立入口通过 |

原始Gazebo Topic使用`/sim/...`命名，`ai_robot_sensors/sensor_adapter`统一改写Frame并以ROS 2传感器QoS发布公共接口。`lidar.launch.py`、`camera.launch.py`、`imu.launch.py`和`encoder.launch.py`可分别启动。编码器默认契约见`config/encoders.yaml`，仿真位置是连续弧度值，2048 ticks/rev作为后续实体适配和量化测试基准。

## 独立诊断

`ai_robot_sensors/sensor_adapter`使用稳态时钟以1 Hz发布`/diagnostics`，检查数据超时、实测频率、时间戳非零且不倒退、Frame匹配和消息内容有效性。无数据为`STALE`，契约无效为`ERROR`，频率超出期望值的50%–150%为`WARN`，全部满足为`OK`。

| 诊断名 | 内容检查 | 运行实测 | 状态 |
|---|---|---|---|
| `sensors/lidar` | 角度/距离范围、ranges非NaN | 9.980 Hz，时间戳/`laser_link`/数据有效 | 通过 |
| `sensors/camera_image` | 分辨率、编码、step和数据长度 | 15.088 Hz，时间戳/`camera_optical_link`/数据有效 | 通过 |
| `sensors/camera_info` | 分辨率和3×3内参矩阵 | 15.094 Hz，时间戳/`camera_optical_link`/数据有效 | 通过 |
| `sensors/imu` | 姿态四元数、角速度和线加速度 | 99.612 Hz，时间戳/`imu_link`/数据有效 | 通过 |
| `sensors/encoder` | 左右轮关节存在，位置/速度有限 | 100.022 Hz，时间戳/`base_link`/数据有效 | 通过 |

## 自动接口测试

`test_sensor_interfaces_launch.py`通过`sensors.launch.py`一次启动雷达、相机、IMU和编码器场景，并已接入`scripts/test.sh`的默认CTest链路。

| 自动断言 | 覆盖内容 | 状态 |
|---|---|---|
| 消息类型 | `LaserScan`、`Image`、`CameraInfo`、`Imu`、`JointState` | 通过 |
| QoS | 四个传感器公共接口BestEffort/Volatile，`/joint_states` Reliable/TransientLocal | 通过 |
| 频率 | 实测值必须位于默认频率的70%–130% | 通过 |
| Frame | `laser_link`、`camera_optical_link`、`imu_link`、`base_link` | 通过 |
| 内容 | 角度/距离、图像尺寸/编码、相机内参、IMU有限值、左右轮位置/速度 | 通过 |
| TF | `base_link -> laser_link`、`base_link -> camera_optical_link`、`base_link -> imu_link`可解析 | 通过 |
| 诊断 | 五个`sensors/*`状态全部为`OK` | 通过 |
| 测试隔离 | 仿真测试使用独立`ROS_DOMAIN_ID`和`IGN_PARTITION` | 通过 |

## 动态契约验证

`test_lidar_noise_imu_motion_and_encoder_accumulation`将动态契约固化在同一launch测试中。测试世界在机器人前方2 m处增加静态雷达靶标。运动命令经`/cmd_vel`进入已有限速和超时停车安全链，不直接绕过到控制器。

| 验收项 | 自动断言 | 状态 |
|---|---|---|
| 雷达噪声 | 取中心波束40个有限样本，均值在1.5–2.0 m，标准差在0.001–0.05 m | 通过 |
| IMU运动响应 | 发布左转命令期间，至少一帧`angular_velocity.z > 0.2 rad/s` | 通过 |
| 编码器前进 | 左右轮累计角度均增加超过1 rad | 通过 |
| 编码器后退 | 左右轮累计角度均减少超过1 rad | 通过 |
| 编码器左转 | 左轮累计量减少、右轮累计量增加，幅度均超过0.5 rad | 通过 |
| 累计量保留 | 完成前进/后退/转向后，左右轮相对初值均有可观测累计变化 | 通过 |

## 当前回归结果

| 检查项 | 结果 | 状态 |
|---|---|---|
| 依赖 | `rosdep check --from-paths src --ignore-src` | 通过 |
| 构建 | 6个ROS 2包构建成功 | 通过 |
| 自动测试 | 26项，0 errors、0 failures、0 skipped | 通过 |
| 编码器独立场景 | `/joint_states`左右轮位置/速度可观测，约100 Hz | 通过 |
| Git基线 | 最新功能与动态验证已提交至`b789710` | 通过 |

## rosbag证据与独立场景

| 验收项 | 实测结果 | 状态 |
|---|---|---|
| 四类rosbag | 雷达、相机、IMU和编码器分别录制，统计与SHA-256见`docs/testing/S1_M3_rosbag_evidence.md` | 通过 |
| 回放 | 四个bag均成功回放主接口并读取正确Frame | 通过 |
| 雷达独立场景 | `lidar.launch.py`发布`/scan`，Frame为`laser_link` | 通过 |
| 相机独立场景 | `camera.launch.py`发布图像与标定信息，Frame为`camera_optical_link` | 通过 |
| IMU独立场景 | `imu.launch.py`发布`/imu/data`，Frame为`imu_link` | 通过 |
| 编码器独立场景 | `encoder.launch.py`发布`/joint_states`，Frame为`base_link` | 通过 |

## 待关闭项

无。

## 安全与范围

- 复用M2底盘安全链，不绕过`/cmd_vel`限速和0.5秒停车。
- 不加载实体传感器或实体电机驱动。
- 不实现`robot_localization`融合、SLAM、Nav2、感知或AI Agent。

## 回退

- S1-M2最终关闭提交：`2337440`。
- S1-M3回退基线：`b789710`。
- 依赖安装、模型占位或单次Topic输出均不能视为M3验收通过。
