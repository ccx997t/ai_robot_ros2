# S1-M5 验收记录

- 验收日期：2026-08-16（启动）
- 工作区：`ai_robot_ros2`
- 阶段：SLAM、定位与Nav2
- 前置基线：S1-M4关闭提交`9d23bb4`
- 当前实施基线：待建立
- 结论：**进行中**；M4前置基线及M5导航依赖准入已通过，场景、配置、自动测试和运行证据尚待建立

## 范围与通过标准

M5在M4运动、感知、融合和安全基线上建立可重复的建图、地图重载、定位与导航能力。完整数据链为：

```text
雷达和里程计契约
→ SLAM建图
→ 地图保存和重载
→ AMCL定位
→ Nav2全局规划
→ 局部控制和避障
→ 恢复行为和任务取消
```

通过标准不是软件包可发现或节点能够启动，而是固定仿真场景中的地图、定位、规划、控制、避障、取消、异常与恢复均具有可重复的自动断言、日志或rosbag证据，并且导航命令始终经过现有安全链。

| 验收对象 | 目标接口/结果 | 验收重点 | 当前状态 |
|---|---|---|---|
| SLAM建图 | `/map`、`map -> odom`、地图保存服务 | 地图尺寸、分辨率、占用内容、TF唯一性、可保存 | 待实现 |
| 地图保存与重载 | 地图图像与YAML、`map_server` | 文件完整性、元数据、重载一致性和Git可追踪摘要 | 待实现 |
| AMCL定位 | `/amcl_pose`、`map -> odom` | 初始位姿、协方差、收敛、偏差和丢失恢复 | 待实现 |
| Nav2全局规划 | `NavigateToPose`及规划结果 | 可达路径、不可达目标、规划耗时和状态反馈 | 待实现 |
| 局部控制与避障 | 安全链输入、局部代价地图、运动结果 | 静态/临时障碍、最小距离、碰撞和重新规划 | 待实现 |
| 任务取消与恢复 | Action取消结果、恢复行为、停车结果 | 取消时限、超时停车、恢复后再次导航 | 待实现 |
| 系统证据 | 自动测试、rosbag、资源指标和Git摘要 | 多次到点、异常场景、全量回归和可复现性 | 待实现 |

## 前置与依赖准入

| 检查项 | 当前结果 | 状态 |
|---|---|---|
| S1-M4 | 最终关闭提交`9d23bb4`；52项测试通过，正常与故障rosbag、资源阈值及安全恢复均已关闭 | 通过 |
| 雷达契约 | `/scan`为`LaserScan`，`laser_link`，约10 Hz，BestEffort/Volatile | 通过 |
| 融合里程计 | `/odom`为融合输出，Frame为`odom`，child为`base_link`，目标30 Hz | 通过 |
| TF基础链 | `odom -> base_link -> sensor_link -> laser_link`已由M4自动验证 | 通过 |
| 底盘安全链 | `/cmd_vel -> cmd_vel_safety_node -> /base_controller/cmd_vel_unstamped`，含限速与0.5秒超时停车 | 通过 |
| `slam_toolbox` | ROS包2.6.10、Debian包`2.6.10-1jammy.20260804.222728`；核心可执行程序可发现 | 依赖准入通过 |
| Nav2与AMCL | ROS包1.1.20；地图、AMCL、规划、控制、行为树、恢复和生命周期组件可发现 | 依赖准入通过 |
| 资源卡 | 已建立`slam_toolbox.md`和`nav2_humble.md`，资源矩阵更新为M5依赖已准入 | 通过 |
| M5仿真世界 | 当前`m2_test.sdf`仅适合固定雷达目标验证 | 待建立 |

已执行的安装命令：

```bash
sudo apt update
sudo apt install \
  ros-humble-slam-toolbox \
  ros-humble-navigation2 \
  ros-humble-nav2-bringup
```

实际版本、许可证、兼容性验证和回退方法已记录到资源卡；以下依赖检查通过：

```bash
source /opt/ros/humble/setup.bash
rosdep check --from-paths src --ignore-src
```

## 初始接口与所有权契约

### TF所有权

- 坐标树保持`map -> odom -> base_link -> sensor_link`。
- 建图模式由`slam_toolbox`独占发布`map -> odom`。
- 定位模式由AMCL独占发布`map -> odom`。
- `ekf_filter_node`继续独占发布`odom -> base_link`。
- `robot_state_publisher`继续发布`base_link`下游静态外参。
- SLAM与AMCL不得同时作为`map -> odom`权威发布者。

### 公共接口

| 接口 | 类型/用途 | 初始契约 |
|---|---|---|
| `/scan` | `sensor_msgs/msg/LaserScan` | SLAM和代价地图的雷达输入，继续复用M3/M4 QoS与Frame |
| `/odom` | `nav_msgs/msg/Odometry` | SLAM、定位和导航的融合里程计输入 |
| `/map` | `nav_msgs/msg/OccupancyGrid` | SLAM输出或地图服务器输出；同一模式仅允许一个权威发布者 |
| `/amcl_pose` | `geometry_msgs/msg/PoseWithCovarianceStamped` | 定位结果及协方差证据 |
| `/initialpose` | `geometry_msgs/msg/PoseWithCovarianceStamped` | AMCL初始位姿输入 |
| `/goal_pose` | `geometry_msgs/msg/PoseStamped` | 人工调试入口，不代替Action验收 |
| `NavigateToPose` | `nav2_msgs/action/NavigateToPose` | 导航目标、反馈、成功、失败和取消的主要能力接口 |
| `/cmd_vel` | `geometry_msgs/msg/Twist` | 导航命令必须进入现有安全节点，不得直达控制器 |
| `/diagnostics` | `diagnostic_msgs/msg/DiagnosticArray` | SLAM、定位、导航和安全异常的统一可观测出口 |

### 模式与启动入口

- 启动文件必须显式支持`mode:=sim|real`，上层地图、定位和导航接口不感知底层模式。
- 建图与定位导航使用独立入口，避免TF和地图发布权冲突。
- `mode:=real`在实体资源未准入前不得启动实体驱动或产生硬件运动输出。
- 初步建议入口为`mapping.launch.py`和`navigation.launch.py`，最终路径在实现时冻结。

## 场景与参数准备

M5需要建立专用导航世界，至少包含封闭边界、非对称地标、走廊、转角、固定障碍物、可插入临时障碍物、多个固定目标点及不可达区域。每个测试点应冻结名称、坐标、朝向和允许误差。

机器人本体尺寸为长0.42 m、宽0.30 m。Nav2配置必须据此冻结矩形`footprint`或保守`robot_radius`，并记录膨胀半径、障碍安全距离、局部/全局代价地图分辨率、最大速度、加速度和控制频率；不得直接采用未经验证的示例参数。

## 验收实施顺序

1. 安装并准入`slam_toolbox`、Nav2和AMCL，补充资源卡及资源矩阵。**已完成。**
2. 建立M5导航世界、固定起点、目标点、静态障碍物和临时障碍物注入方法。
3. 建立建图启动入口与参数，自动验证`/map`、TF、频率和地图内容。
4. 保存地图并验证文件、元数据、哈希及重新加载一致性。
5. 建立AMCL定位入口，验证初始位姿、收敛误差、协方差及`map -> odom`唯一性。
6. 接入Nav2生命周期、全局规划、控制器、行为树和代价地图。
7. 冻结机器人轮廓、速度、加速度、障碍层和膨胀层参数。
8. 自动执行固定路线多次到点，记录成功率、位置误差、耗时和路径长度。
9. 验证静态障碍、临时障碍、最小障碍距离、碰撞次数和重新规划。
10. 验证初始定位偏差、导航取消、雷达超时、不可达目标和恢复后再次导航。
11. 形成正常与异常rosbag、资源基线、关键日志和Git可追踪摘要。
12. 全量执行`rosdep`、构建、自动测试和独立场景回归，关闭M5最终基线。

## 待冻结指标

以下指标必须先通过可重复实测形成初始分布，再冻结最终阈值：

| 指标 | 记录要求 | 当前状态 |
|---|---|---|
| 地图质量 | 分辨率、尺寸、占用/空闲/未知比例、固定结构一致性 | 待测 |
| 定位质量 | 初始收敛时间、位置与角度误差、协方差、丢失和恢复时间 | 待测 |
| 导航到达率 | 固定路线重复次数、成功次数、失败原因 | 待测 |
| 到点精度 | 位置误差、角度误差及允许容差 | 待测 |
| 导航耗时 | 各目标总耗时、规划耗时和重规划耗时 | 待测 |
| 避障安全 | 碰撞次数、最小障碍距离、临时障碍处理结果 | 待测 |
| 取消与停车 | Action取消结果、取消到零速时间、安全节点状态 | 待测 |
| 异常恢复 | 雷达超时、定位异常、不可达目标的结果码、诊断和恢复时间 | 待测 |
| 资源占用 | 完整M5进程树CPU、RSS、关键Topic频率和链路延迟 | 待测 |

## 当前验证汇总

| 验证项 | 当前结果 | 状态 |
|---|---|---|
| M4前置基线 | 提交`9d23bb4`，验收记录结论为通过 | 通过 |
| M5依赖发现 | SLAM与Nav2核心包和可执行程序可发现；`rosdep`全部满足 | 通过 |
| 准入后回归 | 6个包构建完成；52项测试，0错误、0失败、0跳过 | 通过 |
| M5代码与配置 | 尚未建立 | 未执行 |
| M5自动测试 | 尚未建立 | 未执行 |
| M5场景验收 | 尚未建立 | 未执行 |
| M5 rosbag与资源证据 | 尚未形成 | 未执行 |

## 待关闭项

1. 建立M5专用导航世界和固定场景坐标契约。
2. 建立SLAM建图入口、参数和地图质量自动测试。
3. 建立地图保存、Git摘要和重新加载一致性测试。
4. 建立AMCL定位入口及初始偏差、收敛和恢复测试。
5. 建立Nav2全局规划、局部控制、代价地图和生命周期配置。
6. 明确导航命令接入方式，证明任何导航运动均经过现有限速与超时停车安全链。
7. 建立固定路线重复到点、静态/临时障碍和不可达目标自动场景。
8. 建立导航取消、传感器超时、诊断传播和恢复后再次导航测试。
9. 冻结导航成功率、到点误差、耗时、最小障碍距离、定位恢复和资源阈值。
10. 形成正常、取消和异常场景rosbag证据及Git可追踪摘要。
11. 全量执行依赖、构建、测试和场景回归，提交M5最终关闭基线。

## 安全与范围

- 所有导航速度命令必须经过现有急停、限速、0.5秒命令超时和底层保护路径。
- Nav2、测试节点或故障注入器不得直接发布到底盘控制器输入。
- M5故障与运动测试只在仿真模式执行；不启动实体驱动。
- `mode:=real`相关配置只能作为无硬件输出的接口占位，实体运动验收属于后续里程碑。
- M5不实现任务管理、相机目标检测、AI Agent、自然语言或模型调用。

## 证据要求

- 自动测试报告及关键launch测试日志。
- 建图和导航参数、地图文件及SHA-256摘要。
- 正常导航、取消、临时障碍、定位异常和传感器超时的rosbag或等价可回放证据。
- Topic、Action、QoS、TF发布者、频率、时间戳和诊断状态记录。
- 导航成功率、误差、耗时、最小障碍距离、恢复时间及资源占用统计。
- 对应Git提交或标签、已知问题和可执行回退点。

## 回退

- S1-M4最终关闭提交：`9d23bb4`。
- M5任何改动导致M4安全链、融合里程计、雷达、TF或自动回归失败时，不得继续关闭M5验收项。
- 安装依赖、创建目录、单次建图或单次到点均不能作为M5验收通过依据。
